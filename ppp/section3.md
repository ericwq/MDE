# 第 3 部分 薪资案例研究

<center><img src="./imgs/fig-section3-0.png" width="40%"/></center><br/>

我们的第一个主要案例研究时间到了。
我们已经学习了实践和原则，讨论了设计的本质，也谈到了测试和规划。
现在我们需要做一些实际的工作。

在接下来的几章中，我们将探讨一个批量薪资系统的设计和实现。
该系统的初步规范将在后文给出。
作为设计和实现的一部分，我们会发现自己使用了若干不同的设计模式。
这些模式包括 COMMAND、TEMPLATE METHOD、STRATEGY、SINGLETON、NULL OBJECT、FACTORY 和 FACADE 。
这些模式是接下来几章的主题。
然后，在 [第 18 章](../ch18/0.md) 中，我们将完整地完成薪资问题的设计和实现。

阅读本案例研究有几种方式：

- 按顺序通读，先学习设计模式，然后了解它们如何应用于薪资问题。
- 如果你已经了解这些模式并且不打算复习，可以直接跳到 [第 18 章](../ch18/0.md) 。
- 先阅读 [第 18 章](../ch18/0.md) ，然后再回头阅读描述所使用模式的相关章节。
- 分部分阅读 [第 18 章](../ch18/0.md) 。当遇到不熟悉的模式时，先阅读描述该模式的章节，然后再回到 [第 18 章](../ch18/0.md) 。
- 事实上，没有固定的规则。请选择或创造最适合你的策略。

## 薪资系统初步规范

以下是我们与客户沟通时记录的一些要点。

本系统包含公司员工数据库及其相关数据，例如考勤卡。
系统必须支付每位员工的薪资。
员工必须按他们指定的方式，在规定时间内获得正确的工资金额。
此外，还必须从他们的工资中扣除各种款项。

- 部分员工按小时计酬。
他们的员工记录中有一个字段记录小时工资率。
他们提交每日考勤卡，记录日期和工作小时数。
如果他们每天工作超过 8 小时，超出部分按正常工资率的 1.5 倍支付。
他们每周五发薪。

- 部分员工领取固定月薪。
他们在每个月的最后一个工作日发薪。
月薪是员工记录中的一个字段。

- 部分月薪员工还根据销售额获得佣金。
他们提交销售凭条，记录日期和销售额。
佣金率是员工记录中的一个字段。
他们每隔一周的周五发薪。

- 员工可以选择付款方式。
他们可以将工资支票邮寄到指定的邮政地址；也可以将支票留在财务处待领取；或者要求将工资直接存入指定的银行账户。

- 部分员工属于工会。
他们的员工记录中有一个字段记录每周会费。
会费必须从工资中扣除。
此外，工会可能会不时向个别工会会员收取服务费。
这些服务费由工会每周提交，必须从相应员工的下次工资中扣除。

- 薪资应用程序将在每个工作日运行一次，并在当天向符合条件的员工支付工资。
系统将被告知员工工资结算到哪个日期为止，因此它会计算从员工上次发薪日起到指定日期为止的应付工资。

### 练习

在继续之前，你不妨现在自行设计薪资系统，这可能会很有启发。
你可以尝试绘制一些初步的 UML 图。
更好的是，你可以尝试以测试优先的方式实现前几个用例。

运用我们目前学到的原则和实践，尝试创建一个均衡且健康的设计。

如果你打算这样做，那么请查看下面的用例。
否则可以跳过，它们将在薪资章节中再次出现。

#### 用例 1：新增员工

```
通过接收一个 AddEmp 事务来添加新员工。该事务包含员工姓名、地址和分配的员工编号。事务有以下三种形式：

AddEmp <员工ID> "<姓名>" "<地址>" H <小时工资率>

AddEmp <员工ID> "<姓名>" "<地址>" S <月薪>

AddEmp <员工ID> "<姓名>" "<地址>" C <月薪> <佣金率>

员工记录被创建，其各个字段被适当赋值。

**备选流程：**

事务结构错误

如果事务结构不正确，则打印错误信息，不采取任何操作。
```

#### 用例 2：删除员工

```
当收到 DelEmp 事务时，删除员工。该事务格式如下：

DelEmp <员工ID>

收到此事务时，相应的员工记录将被删除。

**备选流程：**

无效或未知的员工ID

如果<员工ID>字段格式不正确，或者它未指向有效的员工记录，则打印该事务并附带错误信息，不采取其他操作。
```

#### 用例 3：登记考勤卡

```
收到 TimeCard 事务后，系统将创建一条考勤卡记录，并将其关联到相应的员工记录。

TimeCard <员工ID> <日期> <小时数>

**备选流程1：**

所选员工不是按小时计酬

系统将打印适当的错误信息，不采取进一步操作。

**备选流程2：**

事务结构错误

系统将打印适当的错误信息，不采取进一步操作。
```

#### 用例 4：登记销售凭条

```
收到 SalesReceipt 事务后，系统将创建一条新的销售凭条记录，并将其关联到相应的佣金制员工。

SalesReceipt <员工ID> <日期> <金额>

**备选流程1：**

所选员工不是佣金制

系统将打印适当的错误信息，不采取进一步操作。

**备选流程2：**

事务结构错误

系统将打印适当的错误信息，不采取进一步操作。
```

#### 用例 5：登记工会服务费

```
收到此事务后，系统将创建一条服务费记录，并将其关联到相应的工会会员。

ServiceCharge <会员ID> <金额>

**备选流程：**

格式错误的事务

如果事务格式不正确，或者<会员ID>未指向现有工会会员，则打印该事务并附带适当的错误信息。
```

#### 用例 6：修改员工详细信息

```
收到此事务后，系统将修改相应员工记录中的某一项详细信息。该事务有若干种可能的变化形式。

ChgEmp <员工ID> Name <姓名> —— 修改员工姓名
ChgEmp <员工ID> Address <地址> —— 修改员工地址
ChgEmp <员工ID> Hourly <小时工资率> —— 改为按小时计酬
ChgEmp <员工ID> Salaried <月薪> —— 改为固定月薪
ChgEmp <员工ID> Commissioned <月薪> <佣金率> —— 改为佣金制
ChgEmp <员工ID> Hold —— 支票暂存待领
ChgEmp <员工ID> Direct <银行> <账户> —— 直接存款
ChgEmp <员工ID> Mail <地址> —— 邮寄支票
ChgEmp <员工ID> Member <会员ID> Dues <会费> —— 加入工会
ChgEmp <员工ID> NoMember —— 退出工会

**备选流程：**

事务错误

如果事务结构不正确，或<员工ID>未指向真实员工，或<会员ID>已指向某位会员，则打印适当的错误信息，不采取进一步操作。
```

#### 用例 7：运行当日薪资发放

```
收到 Payday 事务后，系统查找所有应在指定日期获得薪资的员工。然后系统计算应付金额，并根据所选付款方式支付薪资。

Payday <日期>
```
