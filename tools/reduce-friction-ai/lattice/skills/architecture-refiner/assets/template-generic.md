# 架构精炼器模板（通用）

此模板定义了非整洁架构风格（六边形、模块化单体、自定义等）下 `.lattice/standards/architecture.md` 输出文档的结构。当设置 `architecture_mode: custom` 时使用。它包含访谈指导注释，在最终输出中会被剥离。

产出最终输出时，剥离所有 `<!-- 访谈指导： -->` 注释。最终文档是规范，不是对话记录。

---

## 前置元数据

<!-- 访谈指导：
通用架构文档始终使用覆盖模式——没有嵌入默认值可供叠加。
架构原子将此文档作为唯一参考来读取。
-->

```yaml
---
mode: override
---
```

---

## 引言

<!-- 访谈指导：
包含引言以及所选架构风格名称。
-->

> 这些是 [项目名称] 的架构原则，遵循 [风格名称] 架构。本文档是 `architecture` 原子的唯一参考——没有嵌入默认值。

**目录：**

1. [层级定义](#1-层级定义)
2. [依赖规则](#2-依赖规则)
3. [边界规则](#3-边界规则)
4. [各层级规则](#4-各层级规则)
5. [关键流程](#5-关键流程)
6. [验证清单](#6-验证清单)
7. [反模式](#7-反模式)
8. [歧义信号](#8-歧义信号)

---

## 1. 层级定义

<!-- 访谈指导：
询问："你的架构使用哪些层级？请描述每个层级及其职责。"

对于六边形/端口与适配器，引导对话：
"六边形架构通常有：
- 核心领域——业务规则和领域逻辑
- 端口——定义核心如何与外部通信的接口
- 适配器——将端口连接到真实基础设施（数据库、HTTP、消息）的实现
这与你的项目匹配吗，还是你使用不同的术语？"

对于模块化单体，引导：
"模块化单体通常有垂直切片——每个模块内部拥有自己的层级。
你的模块是什么？在每个模块内，你使用哪些层级？"

对于自定义，询问：
"请描述你架构中的每个层级——其名称、包含什么、负责什么。"

追问问题：
- 你有多少层级？
- 是否有用于横切关注点的共享/通用层？
- 哪些目录映射到每个层级？
- 是否有一个清晰的最内层，没有向外的依赖？

记录：一个包含层级名称、职责和典型目录映射的表。
-->

| 层级 | 职责 | 典型目录 |
|-------|---------------|-------------------|
| （根据访谈填写） | | |

### 目录映射

```
src/
├── （根据访谈填写）
```

---

## 2. 依赖规则

<!-- 访谈指导：
询问："哪些层级可以依赖哪些层级？依赖方向是什么？"

根据风格展示 ASCII 图作为起点：

对于六边形：
"在六边形架构中，典型规则是：
- 适配器依赖端口
- 端口依赖核心领域
- 核心领域不依赖任何东西
这与你的项目匹配吗？"

对于模块化单体：
"在模块化单体中，典型规则是：
- 模块不应依赖其他模块的内部实现
- 模块通过公共契约（API、事件）通信
- 在模块内部，内层没有向外的依赖
这与你的项目匹配吗？"

对于自定义：
"请描述你的依赖规则——哪些层级可以从哪些层级导入？"

追问问题：
- 是否有严格的仅向内规则，还是存在例外？
- 当内层需要触发外层的某些东西时，你如何处理依赖反转？
- 跨边界的数据使用什么格式？（DTO、事件、纯对象）

记录：一个显示依赖方向的 ASCII 图 + 文字规则。
-->

```
（根据访谈填写 ASCII 依赖图）
```

**数据跨边界**：（根据访谈填写——DTO、事件、纯对象等）

---

## 3. 边界规则

<!-- 访谈指导：
询问："你的层级之间如何通信？"

追问问题：
- 层级通过接口、事件、直接方法调用还是混合方式通信？
- 你使用依赖注入吗？什么机制？（DI 容器、手动、框架提供）
- 是否有跨越边界的特定模式？（端口、中介者、事件总线）
- 跨模块通信与跨层通信是否不同？

记录：通信模式、DI 方法以及任何跨边界约定。
-->

（根据访谈填写）

---

## 4. 各层级规则

<!-- 访谈指导：
逐个浏览 §1 中的每个层级。对于每一层，询问：
"这一层允许什么？禁止什么？"

对于每一层，捕获：
- 属于这里的内容（项目符号列表）
- 不属于这里的内容（项目符号列表）
- 需要注意的常见违规（项目符号列表）

此章节至关重要——它提供了原子所需的执行细节。
-->

### （层级 1 名称）

**属于这里的内容：**
- （根据访谈填写）

**不属于这里的内容：**
- （根据访谈填写）

**常见违规：**
- （根据访谈填写）

### （层级 2 名称）

**属于这里的内容：**
- （根据访谈填写）

**不属于这里的内容：**
- （根据访谈填写）

**常见违规：**
- （根据访谈填写）

<!-- 访谈指导：
对 §1 中识别的每个层级重复此子章节模式。
-->

---

## 5. 关键流程

<!-- 访谈指导：
询问："你能带我走一遍你架构中 1-2 个代表性的流程吗？"

建议的提示：
- "一个写操作（例如，创建资源）如何流经你的层级？"
- "一个读操作如何流经你的层级？"
- "一个跨模块或跨边界的操作如何工作？"

对于每个流程，捕获：
- 流程名称（例如，"写操作"、"读操作"）
- 逐层的步骤路径
- 每步由哪一层负责什么

记录：命名的流程及其逐层遍历，使用伪代码或流程图。
-->

### 流程 1：（名称）

```
（根据访谈填写逐步流程）
```

### 流程 2：（名称）

```
（根据访谈填写逐步流程）
```

---

## 6. 验证清单

<!-- 访谈指导：
此章节是必需的——架构原子读取它来运行生成后验证。
原子使用命令式的 STOP 并验证语言来使用此列表。它必须包含至少 3 个具体检查项。

询问："AI 在生成代码后应该检查什么以确保代码遵循你的架构？"

用适配用户层级的通用结构检查来引导：
- 每个类/模块是否在正确的层级中？
- 依赖方向是否遵循 §2 中的规则？
- 跨边界的数据是否使用 §3 中的模式？

然后询问风格特定的检查：
"是否有你的架构风格特有的额外检查？"

格式：带标签项的有序列表（例如，"1. 层级位置：……"）。不是复选框。
原子将在生成每个组件后停止，并按顺序遍历此列表。
最少：3 项。争取 5-8 项以获得全面覆盖。
-->

在生成每个组件后停止。在继续之前验证以下所有项：

1. （根据访谈填写——例如，**层级位置**：每个类是否在正确的层级中？）
2. （根据访谈填写——例如，**依赖方向**：所有依赖是否遵循上述规则？）
3. （根据访谈填写——例如，**边界数据**：跨层数据是否使用正确的格式？）

---

## 7. 反模式

<!-- 访谈指导：
此章节是必需的——架构原子读取它来运行主动反模式扫描。
原子扫描生成的代码中是否存在这里列出的每个模式。它必须包含至少 3 个反模式。

询问："在你的风格中，AI 应该注意的最常见的架构错误是什么？"

用适配用户风格的通用反模式来引导：
- 错误层级中的逻辑
- 错误方向的依赖
- 边界违规（跨层泄露类型）
- 跨越多个层级的上帝类

然后询问风格特定的反模式：
"是否有 [六边形/模块化单体/你的风格] 特有的应被标记的模式？"

对于每个反模式，询问："当 AI 发现此模式时，它应该采取什么措施来修复？"
这至关重要——原子需要症状（检测什么）和修复（如何处理）。

格式：包含模式名称、症状和修复操作的复选框列表。
最少：3 个反模式。争取 5-8 个以获得全面覆盖。
-->

在验证上述清单后，扫描输出中是否存在这些反模式。如果发现，先修复再呈现。

- [ ] **（模式名称）**：（症状）→ （修复操作）
- [ ] **（模式名称）**：（症状）→ （修复操作）
- [ ] **（模式名称）**：（症状）→ （修复操作）

---

## 8. 歧义信号

<!-- 访谈指导：
此章节是可选的，但推荐使用。它帮助 AI 知道何时应该发问而不是默默选择。

询问："在你的风格中，是否存在多种有效方法的架构决策？
例如，一个组件可以合理地存在于两个不同层级中，或者一个流程
可以用直接调用或事件来实现。"

用通用架构歧义来引导：
- 一个组件可以属于层 A 或层 B——什么决定答案？
- 跨边界操作可以使用同步调用或事件——何时使用哪种？
- 数据结构可以在层级间共享或重复——团队的偏好是什么？

然后询问风格特定的歧义：
"在 [六边形/模块化单体/你的风格] 中，工程师意见不一致的灰色地带是什么？"

格式：描述歧义场景及其为什么构成判断决策的项目符号列表。
当 AI 在生成过程中遇到其中之一时，它应该呈现选项而不是默默选择。
-->

这些检查通常有多种有效结果。当遇到时，呈现选项而不是默默选择。

- （根据访谈填写——例如，一个可以存在于两个层级中任何一个的组件）

---

## 新章节

<!-- 访谈指导：
在访谈结束时，询问：
"是否有你想添加的项目特定章节？常见添加：
- 命名约定（文件命名、类命名模式）
- 框架特定规则（例如，NestJS 模块结构、Spring 约定）
- 测试模式（测试如何与架构层级对齐）
- 错误处理模式（错误如何跨层传播）
- 事件模式（事件命名、载荷结构、何时使用事件）"

如果用户想添加章节，从 9 开始编号。
-->

---

## 页脚

<!-- 访谈指导：
在输出中包含项目名称、生成日期和风格名称。
示例：

---
*为 [项目名称] 于 [日期] 生成。风格：[风格名称]。*
*由架构精炼器技能生成。*
-->
