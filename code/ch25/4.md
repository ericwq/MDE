## 结论

这就是你在软件架构中画边界线的方式。
你将系统划分为组件。
其中一些组件是核心业务规则。
另一些是插件，包含与核心业务不直接相关的必要功能。
然后，你安排这些组件中的代码，使得它们之间的箭头指向一个方向——朝向核心业务。

你应该认识到，这是依赖反转原则（DIP）和稳定抽象原则（SAP）的应用。
依赖箭头被安排为从较低层的细节指向较高层的抽象。

这就是 *架构的依赖规则 (Dependency Rule of Architecture)* ：

> 跨越架构边界的依赖关系必须始终指向较高层的一侧。
