# 第 2 章 清理那些代码！

<center><img src="../imgs/fig-ch2-0.png" width="40%"></center><br/>

整洁代码！那是一个名词吗？还是一个命令？
clean 这个词是形容词还是动词？
在本章以及整本书中，我们采取后一种观点。
这是一章关于清理代码的书，也是一本关于清理代码的书。

一个人不是简单地写出整洁代码。
要创建干净的代码，必须清理它。
<ins>代码不会以干净的状态从你的脑海中涌出并穿过你的手指流出。
我们的大脑不是那样工作的。
这是因为整洁性和功能性是相互竞争的正交关注点，而我们这些凡人并不特别擅长平衡它们。
相反，我们有限的大脑首先专注于功能性的关注点，努力生成能工作的代码。
只有当这个目标实现后，我们才能将注意力转向清理我们刚刚制造的混乱</ins>。

或者，正如 Kent Beck 简洁地说的那样：

> 首先，让它工作。然后，让它正确。

当我们做第一部分 ——让它工作时—— 我们通常也会制造一团糟。
这是因为让它工作需要我们所有的认知能量，没有剩余的能量去思考其他任何事情。

一旦我们有了能工作的东西，然后我们就可以退后一步，审视它，并思考改进它的方法。

你们都有过审视自己过去编写的代码的经历 ——也许只是在几天或几个小时前—— 然后意识到可以通过这样或那样的更改来改进它的经历。

你也很有可能对你做过的几乎所有事情都有过同样的经历。
如果你曾写过一篇论文、一个故事或一篇文章，你很可能创建了草稿，然后检查了一遍、两遍或三遍来改进它们。
如果你画过画，你很可能花了很多时间使用橡皮擦。
事实上，我已经把最后几段重读了好几遍，每次都有一些小改进。

<ins>这就是我们人类的工作方式。
我们一开始会产生相当丑陋的东西，然后我们反复推敲和调整，直到它变得更好</ins>。

或许一个例子会有所帮助。
我写了一个简单的 ¹ 从罗马数字转换为整数的转换器。
我在 GitHub Copilot 的帮助下，通过简单地将代码抛入一个函数并摸索，直到我认为它工作了。
我也有一些 Copilot 和我同样拼凑起来的单元测试，但坦率地说，我不确定它们是否覆盖了所有情况。
这是代码。我预计它会让你的眼睛翻白，当你惊恐地盯着它时，会让你的脸色变得苍白。

¹ 按照 “简单” 这个词的某种定义。

#### Listing 2-1
```java
package fromRoman;

import java.util.Arrays;

public class FromRoman {

    public static int convert(String roman) {
        if (roman.contains("VIV")
                || roman.contains("IVI")
                || roman.contains("IXI")
                || roman.contains("LXL")
                || roman.contains("XLX")
                || roman.contains("XCX")
                || roman.contains("DCD")
                || roman.contains("CDC")
                || roman.contains("MCM")) {
            throw new InvalidRomanNumeralException(roman);
        }

        roman = roman.replace("IV", "4");
        roman = roman.replace("IX", "9");
        roman = roman.replace("XL", "F");
        roman = roman.replace("XC", "N");
        roman = roman.replace("CD", "G");
        roman = roman.replace("CM", "O");

        if (roman.contains("IIII")
                || roman.contains("VV")
                || roman.contains("XXXX")
                || roman.contains("LL")
                || roman.contains("CCCC")
                || roman.contains("DD")
                || roman.contains("MMMM")) {
            throw new InvalidRomanNumeralException(roman);
        }

        int[] numbers = new int[roman.length()];
        int i = 0;
        for (char digit : roman.toCharArray()) {
            switch (digit) {
                case 'I' -> numbers[i] = 1;
                case 'V' -> numbers[i] = 5;
                case 'X' -> numbers[i] = 10;
                case 'L' -> numbers[i] = 50;
                case 'C' -> numbers[i] = 100;
                case 'D' -> numbers[i] = 500;
                case 'M' -> numbers[i] = 1000;
                case '4' -> numbers[i] = 4;
                case '9' -> numbers[i] = 9;
                case 'F' -> numbers[i] = 40;
                case 'N' -> numbers[i] = 90;
                case 'G' -> numbers[i] = 400;
                case 'O' -> numbers[i] = 900;
                default -> throw new InvalidRomanNumeralException(roman);
            }
            i++;
        }

        int lastDigit = 1000;
        for (int number : numbers) {
            if (number > lastDigit) {
                throw new InvalidRomanNumeralException(roman);
            }
            lastDigit = number;
        }

        return Arrays.stream(numbers).sum();
    }

    public static class InvalidRomanNumeralException extends RuntimeException {
        public InvalidRomanNumeralException(String roman) {
            super(roman);
        }
    }
}
```

> **Future Bob**：两个月后我读这段代码，看到其中一些部分（比如 `49FNGO`）有多糟糕，我正歇斯底里地大笑……然而其中一些部分又多么有洞察力。

以下是我使用的测试。我怀疑它们是否完整。

```java
package fromRoman;

import fromRoman.FromRoman.InvalidRomanNumeralException;
import org.junit.jupiter.api.Test;
import static fromRoman.FromRoman.convert;
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.is;
import static org.junit.jupiter.api.Assertions.assertThrows;

public class FromRomanTest {

    @Test
    public void valid() throws Exception {
        assertThat(convert(""), is(0));
        assertThat(convert("I"), is(1));
        assertThat(convert("II"), is(2));
        assertThat(convert("III"), is(3));
        assertThat(convert("IV"), is(4));
        assertThat(convert("V"), is(5));
        assertThat(convert("VI"), is(6));
        assertThat(convert("VII"), is(7));
        assertThat(convert("VIII"), is(8));
        assertThat(convert("IX"), is(9));
        assertThat(convert("X"), is(10));
        assertThat(convert("XI"), is(11));
        assertThat(convert("XII"), is(12));
        assertThat(convert("CXC"), is(190));
    }

    @Test
    public void invalid() throws Exception {
        assertInvalid("IIII");
        assertInvalid("VV");
        assertInvalid("XXXX");
        assertInvalid("LL");
        assertInvalid("CCCC");
        assertInvalid("DD");
        assertInvalid("MMMM");
        assertInvalid("IIV");
        assertInvalid("IVI");
        assertInvalid("IXI");
        assertInvalid("VIV");
        assertInvalid("XVX");
        assertInvalid("XIIII");
        assertInvalid("XVV");
        assertInvalid("XIVI");
        assertInvalid("XIXI");
        assertInvalid("XVIV");
        assertInvalid("LXL");
        assertInvalid("XLX");
        assertInvalid("XCX");
        assertInvalid("CDC");
        assertInvalid("DCD");
        assertInvalid("MCMC");
        assertInvalid("MCDM");
    }

    private void assertInvalid(String r) {
        assertThrows(InvalidRomanNumeralException.class, () -> convert(r));
    }
}
```

我希望你同意我的看法，这需要一些清理。它感觉粗糙而混乱。
在本章稍后以及接下来的章节中，我将逐步讲解清理过程。
但现在，我只想向你展示结果。

在我展示结果之前，我想说，清理代码所花费的时间大约与最初编写代码的时间相当。
Copilot 在初始编写过程中提供了一定的帮助，但在清理过程中帮助要少得多。

在清理过程中，仅仅通过整理我的思路以及代码和测试的结构，我就发现了一两个小错误。
第二次审视能发现什么真是令人惊讶 ² —— 而清理是终极的第二次审视。

² 在我对飞机进行近距离飞行前检查后，我会退后一步，绕着飞机走一圈，从更远的视角进行第二次审视。
通过这种方法发现的东西真是令人惊讶。
有些东西在远处显而易见，但在近距离检查时几乎看不见。

```java
package fromRoman;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;

public class FromRoman {
    private String roman;
    private final List<Integer> numbers = new ArrayList<>();
    private int charIx;
    private char nextChar;
    private Integer nextValue;
    private Integer value;
    private int nchars;

    private final Map<Character, Integer> values = Map.of(
            'I', 1,
            'V', 5,
            'X', 10,
            'L', 50,
            'C', 100,
            'D', 500,
            'M', 1000
    );

    public FromRoman(String roman) {
        this.roman = roman;
    }

    public static int convert(String roman) {
        return new FromRoman(roman).doConversion();
    }

    private int doConversion() {
        checkInitialSyntax();
        convertLettersToNumbers();
        checkNumbersInDecreasingOrder();
        return numbers.stream().reduce(0, Integer::sum);
    }

    private void checkInitialSyntax() {
        checkForIllegalPrefixCombinations();
        checkForImproperRepetitions();
    }

    private void checkForIllegalPrefixCombinations() {
        checkForIllegalPatterns(new String[]{
                "VIV", "IVI", "IXI", "IXV",
                "LXL", "XLX", "XCX", "XCL",
                "DCD", "CDC", "CMC", "CMD"
        });
    }

    private void checkForImproperRepetitions() {
        checkForIllegalPatterns(new String[]{
                "IIII", "VV", "XXXX", "LL",
                "CCCC", "DD", "MMMM"
        });
    }

    private void checkForIllegalPatterns(String[] patterns) {
        for (String badString : patterns) {
            if (roman.contains(badString)) {
                throw new InvalidRomanNumeralException(roman);
            }
        }
    }

    private void convertLettersToNumbers() {
        char[] chars = roman.toCharArray();
        nchars = chars.length;

        for (charIx = 0; charIx < nchars; charIx++) {
            nextChar = isLastChar() ? 0 : chars[charIx + 1];
            nextValue = values.get(nextChar);
            char thisChar = chars[charIx];
            value = values.get(thisChar);

            switch (thisChar) {
                case 'I' -> addValueConsideringPrefix('V', 'X');
                case 'X' -> addValueConsideringPrefix('L', 'C');
                case 'C' -> addValueConsideringPrefix('D', 'M');
                case 'V', 'L', 'D', 'M' -> numbers.add(value);
                default -> throw new InvalidRomanNumeralException(roman);
            }
        }
    }

    private boolean isLastChar() {
        return charIx + 1 == nchars;
    }

    private void addValueConsideringPrefix(char p1, char p2) {
        if (nextChar == p1 || nextChar == p2) {
            numbers.add(nextValue - value);
            charIx++;
        } else {
            numbers.add(value);
        }
    }

    private void checkNumbersInDecreasingOrder() {
        for (int i = 0; i < numbers.size() - 1; i++) {
            if (numbers.get(i) < numbers.get(i + 1)) {
                throw new InvalidRomanNumeralException(roman);
            }
        }
    }

    public static class InvalidRomanNumeralException extends RuntimeException {
        public InvalidRomanNumeralException(String roman) {
            super("Invalid Roman numeral: " + roman);
        }
    }
}
```

我认为这干净多了。
在我看来，使其更干净的是那些简短、命名良好的函数，命名良好的实例变量，以及函数按照被调用的顺序列出的这一事实。
你可以从上到下阅读代码，它读起来相当像一个故事。

> **Future Bob**：两个月后我有些纠结。
第一个版本，尽管丑陋，但没有像这个版本这样被切分得支离破碎。
诚然，提取出的函数的名称和顺序读起来像一个故事，对理解意图有很大帮助；但有几次我不得不向上滚动到顶部，以确认实例变量的类型。
我发现这种支离破碎和滚动是令人恼火的。
然而，关键的一点是，我是在先阅读了丑陋的版本并经历了理解它的工作之后，才阅读这个清理过的代码的。
所以现在，当我阅读这个版本时，我感到恼火，因为我已经理解了它，并且觉得那些被切分的函数和实例变量是多余的。
>
> 不要误解我的意思，我仍然认为更干净的版本更好。
我只是没有预料到这种恼火。
当我第一次清理它时，我认为它会没有烦恼。
>
> 我想你应该问自己的问题是，你更愿意先阅读这两段代码中的哪一段。
哪一段更能告诉你意图？哪一段模糊了意图？
在这方面，后者当然是更好的。
>
> 这个烦恼是 John Ousterhout³ 和我一直在争论的问题。⁴ 当你理解一个算法时，那些旨在帮助你理解它的工件会变得令人烦恼。
更糟糕的是，如果你理解了一个算法，你为帮助他人而写的名称或注释将被那种理解所偏见，可能不会像你认为的那样对读者有帮助。
在这段代码中，一个很好的例子是 `addValueConsideringPrefix` 这个函数。
当我理解算法时，这个名字对我来说完全合理。
但在两个月后，它有点令人不适。
也许不像 `49FNGO` 那样令人不适，但仍然不像我写它时希望的那样明显。
或许写成 `numbers.add(decrementValueIfThisCharIsaPrefix);` 会更好，因为那将与无前缀情况下的 `numbers.add(value);` 对称。

³ [APOD](../bib.md#apod) 。<br/>
⁴ 参见附录阅读那次辩论。

> 底线是，你自己对你正在清理的内容的理解，将不利于你与下一个接手的人沟通的能力。
无论你是提取命名良好的方法，还是添加描述性的注释，情况都是如此。
因此，在选择名称和编写注释时要特别小心；如果其他人对你的选择感到恼火，也不要感到惊讶。
最后，几个月后的审视既会让人谦卑，也会让人受益。

`convertLettersToNumbers` 这个函数比我喜欢的略长一些；但它整体上结合得很好，我认为将其分解为更小的函数会更模糊而不是更有帮助。

你们中的一些人可能会以更怀疑的眼光看待这段 “更干净” 的代码。
也许你有一些问题或抱怨。

你可能会抱怨它更长。
这是真的；但大部分额外的长度来自于那些命名良好的函数，它们解释了正在发生的事情。

你可能会抱怨创建一个对象是过度杀伐且昂贵的。
确实，分配内存然后稍后垃圾回收需要额外的时间。
调用所有这些小方法也花费时间。
如果你正在编写具有非常严格时序约束的嵌入式实时代码，或者如果你正在为高性能飞行模拟器或第一人称射击游戏编写代码，那么这个决定可能看起来是疯狂的，更不用说是不整洁了。
但就我的目的而言，额外的时间是我最不关心的。
我不需要速度。
对于我的情况，我认为在合理的范围内，用速度换取清晰度是一个好的权衡。

你可能是一个对函数不 “纯粹” 感到震惊的函数式程序员。
但实际上，静态的 `convert` 函数已经是尽可能纯粹的函数了。
其他的只是在该总体的纯粹函数单次调用内运行的小助手。
那些实例变量非常方便，允许各个方法无需诉诸传递参数就能通信。
这表明对象的一个良好用途是允许在纯函数执行内部运行的那些辅助函数，通过对象的实例变量轻松地通信。

所以我认为这相当干净。
我说得对吗？我认为我是对的；但你可能不同意。
没关系。我们允许有不同意见。
对于什么是整洁，没有单一的标准。
这是我的，我的可能不是你的。我只会要求你考虑我的，并发展出你自己的标准。
最重要的是要有一个你严格应用的标准。

如果你是一个团队的成员，那么团队应该有一个单一的整洁标准 ⁵，由团队所有成员协商、遵循和支持。

⁵ 这可能会诱惑你创建一份标准文档。
我建议不要这样做。
代表标准的文档应该是代码本身。
只有在代码目前不体现标准的情况下，才有必要在代码之外有一份标准文档。

顺便说一句，我最初编写的测试是不充分的。
在我进行清理的过程中，我发现了一些需要进一步测试的情况。
为了完整性，我在下面包含了最终的测试。

```java
package fromRoman;

import fromRoman.FromRoman.InvalidRomanNumeralException;
import org.junit.jupiter.api.Test;
import static fromRoman.FromRoman.convert;
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.is;
import static org.junit.jupiter.api.Assertions.assertThrows;

public class FromRomanTest {

    @Test
    public void valid() throws Exception {
        assertThat(convert(""), is(0));
        assertThat(convert("I"), is(1));
        assertThat(convert("II"), is(2));
        assertThat(convert("III"), is(3));
        assertThat(convert("IV"), is(4));
        assertThat(convert("V"), is(5));
        assertThat(convert("VI"), is(6));
        assertThat(convert("VII"), is(7));
        assertThat(convert("VIII"), is(8));
        assertThat(convert("IX"), is(9));
        assertThat(convert("X"), is(10));
        assertThat(convert("XI"), is(11));
        assertThat(convert("XII"), is(12));
        assertThat(convert("XIII"), is(13));
        assertThat(convert("XIV"), is(14));
        assertThat(convert("XV"), is(15));
        assertThat(convert("XVI"), is(16));
        assertThat(convert("XIX"), is(19));
        assertThat(convert("XX"), is(20));
        assertThat(convert("XXX"), is(30));
        assertThat(convert("XL"), is(40));
        assertThat(convert("L"), is(50));
        assertThat(convert("LX"), is(60));
        assertThat(convert("LXXIV"), is(74));
        assertThat(convert("XC"), is(90));
        assertThat(convert("C"), is(100));
        assertThat(convert("CXIV"), is(114));
        assertThat(convert("CXC"), is(190));
        assertThat(convert("CD"), is(400));
        assertThat(convert("D"), is(500));
        assertThat(convert("CDXLIV"), is(444));
        assertThat(convert("DCXCIV"), is(694));
        assertThat(convert("CM"), is(900));
        assertThat(convert("M"), is(1000));
        assertThat(convert("MCM"), is(1900));
        assertThat(convert("MCMXCIX"), is(1999));
        assertThat(convert("MMXXIV"), is(2024));
    }

    @Test
    public void invalid() throws Exception {
        assertInvalid("IIII");
        assertInvalid("VV");
        assertInvalid("XXXX");
        assertInvalid("LL");
        assertInvalid("CCCC");
        assertInvalid("DD");
        assertInvalid("MMMM");
        assertInvalid("XIIII");
        assertInvalid("LXXXX");
        assertInvalid("DCCCC");
        assertInvalid("VIIII");
        assertInvalid("MCCCC");
        assertInvalid("VX");
        assertInvalid("IIV");
        assertInvalid("IVI");
        assertInvalid("IXI");
        assertInvalid("IXV");
        assertInvalid("VIV");
        assertInvalid("XVX");
        assertInvalid("XVV");
        assertInvalid("XIVI");
        assertInvalid("XIXI");
        assertInvalid("XVIV");
        assertInvalid("LXL");
        assertInvalid("XLX");
        assertInvalid("XCX");
        assertInvalid("XCL");
        assertInvalid("CDC");
        assertInvalid("DCD");
        assertInvalid("CMC");
        assertInvalid("CMD");
        assertInvalid("MCMC");
        assertInvalid("MCDM");
    }

    private void assertInvalid(String r) {
        assertThrows(InvalidRomanNumeralException.class, () -> convert(r));
    }
}
```

测试的数量之多有点令人生畏。
在有效的测试中可能存在一些冗余。
我相当肯定在无效测试中没有冗余。

> **Future Bob**：我有点恼火我没有找到一种更抽象的方法来确定和测试无效条件。
我相信一定存在一种。
