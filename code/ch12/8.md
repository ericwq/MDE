## 但性能呢？

可以公平地说，今天的编译器可以利用 `switch` 语句的局部性和特定的处理器架构来生成比多态分发运行得更快的可执行代码。
因此，OO 设计可能会产生轻微的性能损失。
如果你正在处理一个项目中，零星纳秒至关重要，那么你可能不得不放弃 OCP 和 DIP，转而使用 `switch` 语句而不是多态。

<ins>大多数程序员不需要关心零星的纳秒。
但如果你确实需要，小心地将它们集中在对你最有利的地方。
不要仅仅因为少数时间关键的内循环必须使用 `switch` 就在整个系统中放弃 OO 设计</ins>。
