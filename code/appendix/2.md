## 方法长度

**JOHN：**<br/>
我们第一个分歧领域是方法长度。
在《Clean Code》第 34 页，你说：“函数的第一条规则是它们应该小。函数的第二条规则是它们应该比那更小。”
后来，你说：“函数几乎不应超过 20 行长”，并建议函数应该 “只有两、三或四行长”。
在第 35 页，你说：“if 语句、else 语句、while 语句等内部的块应该只有一行长。很可能那一行应该是一个函数调用。”
我在《Clean Cod3》中找不到任何表明函数可能太短的内容。

我同意将代码划分为相对较小的单元（模块化设计）是减少程序员必须一次性在脑海中记住的信息量的最重要方法之一。
这个想法当然是，将一个复杂的功能块封装在一个具有简单接口的单独方法中。
然后，开发人员可以利用该方法的功能（或阅读调用该方法的代码），而无需学习该方法是如何实现的细节；他们只需要学习它的接口。
最好的方法是那些提供大量功能但具有非常简单的接口的方法：它们用一个更小的认知负荷（学习接口）取代了一个大的认知负荷（阅读详细的实现）。
我把这些方法称为 *深层的 (deep)* 。

然而，像软件设计中的大多数想法一样，分解可能做得过头。
随着方法变得越来越小，进一步细分的收益越来越少。
每个接口后面隐藏的功能量下降，而接口往往变得更加复杂。
我把这些接口称为 *浅层的 (shallow)* ：它们在减少程序员需要知道的东西方面没有多大帮助。
最终，达到了这样的程度：使用该方法的人需要理解其实现的每个方面。
这样的方法通常是无意义的。

过度分解的另一个问题是它往往导致 *纠缠 (entanglement)* 。
如果为了理解其中一个方法的内部工作原理，你还需要阅读另一个方法的代码，那么这两个方法就是纠缠的（在 APOSD 术语中是连体的）。
如果你在阅读代码时发现自己来回翻看两个方法的实现，那就是一个危险信号，表明这些方法可能是纠缠的。
纠缠的方法很难阅读，因为你需要在脑中同时拥有的信息并不都在同一个地方。
纠缠的方法通常可以通过组合它们来改进，以便所有代码都在一个地方。

《Clean Code》中关于方法长度的建议如此极端，以至于它鼓励程序员创建微小的方法，这些方法同时遭受浅层接口和纠缠的困扰。
设置武断的数字限制，比如方法中两到四行，if 或 while 语句体中一行，会加剧这个问题。

**UB：**<br/>
虽然我确实强烈推荐非常短的函数，但我认为说这本书设置了武断的数字限制是不公平的。
你在第 34 页提到的两到四行函数是 Kent Beck 和我 1999 年为了学习 TDD 作为练习一起编写的 Sparkle 小程序的一部分。
我认为那个小程序中的大多数函数都是两到四行长，这是值得注意的，因为它是一个 Swing 程序；而 Swing 程序往往有非常长的方法。

至于设置限制，在第 13 页我明确指出，尽管书中的建议对我和其他作者来说效果很好，但它们可能对每个人都不适用。
我没有声称拥有最终权威，甚至没有任何绝对的 “正确性”。
它们只是供参考。

**JOHN：**<br/>
我认为如果我们看具体的代码示例，这些问题最容易理解。
但在此之前，让我问你，Bob：你认为代码有可能被过度分解吗，还是越小越好？
如果你认为过度分解是可能的，你如何识别它何时发生？

**UB：**<br/>
当然有可能过度分解代码。
这里有一个例子：

```java
void doSomething() {doTheThing()} // 过度分解了。
```

我用来决定分解到何种程度的策略是古老的规则：一个方法应该做一件事。
如果我能有意义地从另一个方法中提取一个方法，那么原始方法就做了不止一件事。
“有意义” 意味着提取的功能可以被赋予一个描述性的名称，并且它比原始方法做得少。

**JOHN：**<br/>
不幸的是，“一件事” 的方法会导致过度分解：

“一件事” 这个术语是模糊的，容易被滥用。
例如，如果一个方法有两行代码，它不是在两件事吗？

你没有提供任何有用的护栏来防止过度分解。
你给出的例子太极端了，没什么用，而 “能否命名” 的条件也没有帮助：任何东西都可以命名。

“一件事” 的方法在许多情况下是完全错误的。
如果两件事是紧密相关的，那么在一个单一方法中实现它们很可能是有意义的。
例如，任何线程安全的方法首先必须获取一个锁，然后执行其功能。
这是两件 “事”，但它们属于同一个方法。

**UB：**<br/>
让我先处理最后一件事。
你建议锁定线程和执行临界区应该放在同一个方法中。
然而，我倾向于将锁定与临界区分开。

```java
void concurrentOperation() {
    lock()
    criticalSection();
    unlock()
}
```

这解耦了临界区和锁，并允许在不需要锁定的情况下（例如，在单线程模式下）或当锁已被其他人设置时调用它。

现在，回到 “容易被滥用” 的论点。
我不认为这是一个重要的担忧。
If 语句容易被滥用。
Switch 语句容易被滥用。
赋值语句容易被滥用。
某物容易被滥用并不意味着它应该被避免或压制。
它只是意味着人们应该采取适当的谨慎态度。
总会有这种叫做判断力的东西。

所以，当面对较大方法中的这段代码片段时：

```java
...
amountOwed=0;
totalPoints=0;
...
```

将它们提取如下将是糟糕的判断，因为提取没有意义。
实现并不比接口更深入细节。

```java
void clearAmountOwed() {
    amountOwed=0;
}
void clearTotalPoints() {
    totalPoints=0;
}
```

然而，将它们提取如下可能是好的判断，因为接口是抽象的，并且实现有更深的细节。

```java
void clearTotals() {
    amountOwed=0;
    totalPoints=0;
}
```

**JOHN：**<br/>
当然，任何东西都可能被滥用。
但最好的设计方法是鼓励人们以正确的方式做事，并阻止滥用。
不幸的是，“一件事” 规则出于我上面给出的原因，鼓励了滥用。

当然，软件设计者需要运用判断力：不可能为软件设计提供精确的配方。
但好的判断需要原则和指导。
《Clean Code》中关于分解的论点，包括 “一件事” 规则，是片面的。
它们给出了关于何时切割的强烈、具体、定量的建议，几乎没有任何关于如何判断你走得太远的指导。
我唯一能找到的是第 36 页关于清单 3-3 的一个两句话的例子（这相当琐碎），埋在 “切，切，切” 的劝诫中间。

我使用深层/浅层特征的原因之一是它抓住了权衡的两面；它会告诉你分解何时是好的，也告诉你分解何时会使事情变得更糟。

**UB：**<br/>
你说得很对，我在书中没有太多讨论如何做出判断。
在 2008 年，我关心的是打破在那些早期 Web 时代常见的大型函数的习惯。
在第二版中，我已经更加平衡了。

尽管如此，如果我必须犯错，我宁愿在分解方面犯错。
考虑和可视化分解是有价值的。
如果我们判断它们走得太远，它们总是可以被内联。

**JOHN：**<br/>
回到你的 clearTotals 例子：

- clearTotals 方法似乎与 “一件事” 规则相矛盾：变量 amountOwed 和 totalPoints 似乎没有特别的关系，所以同时初始化它们不是在两件事吗？
你说两个语句都在执行初始化，这使得它只是一件事（初始化）。
这是否意味着有一个单一方法初始化两个完全没有共同点的独立对象也是可以的？
我怀疑不是。
感觉你正在努力为应用 “一件事” 规则创建一个清晰的框架；这让我认为它不是一个好的规则。

- 如果没有看到更多的上下文，我怀疑 clearTotals 方法是否有意义。

**UB：**<br/>
我希望你同意，在这两个例子之间，前者更好一些。

```java
public String makeStatement() {
    clearTotals();
    return makeHeader() + makeRentalDetails() + makeFooter();
}
```

```java
public String makeStatement() {
    amountOwed=0;
    totalPoints=0;
    return makeHeader() + makeRentalDetails() + makeFooter();
}
```

**JOHN：**<br/>
嗯，实际上，不。
第二个例子完全清晰和明显：我不认为拆分它有什么好处。

SPOCK（又名 UB）：<br/>
引人入胜。

**JOHN：**<br/>
我认为如果我们考虑一个非平凡的代码示例，将更容易澄清我们的分歧。
让我们看看《Clean Code》中的 PrimeGenerator 类，它是第 145-146 页的清单 10-8。
这个 Java 类生成前 N 个质数：

```java
package literatePrimes;
import java.util.ArrayList;

public class PrimeGenerator {
    private static int[] primes;
    private static ArrayList<Integer> multiplesOfPrimeFactors;

    protected static int[] generate(int n) {
        primes = new int[n];
        multiplesOfPrimeFactors = new ArrayList<Integer>();
        set2AsFirstPrime();
        checkOddNumbersForSubsequentPrimes();
        return primes;
    }

    private static void set2AsFirstPrime() {
        primes[0] = 2;
        multiplesOfPrimeFactors.add(2);
    }

    private static void checkOddNumbersForSubsequentPrimes() {
        int primeIndex = 1;
        for (int candidate = 3;
             primeIndex < primes.length;
             candidate += 2) {
            if (isPrime(candidate))
                primes[primeIndex++] = candidate;
        }
    }

    private static boolean isPrime(int candidate) {
        if (isLeastRelevantMultipleOfLargerPrimeFactor(candidate)) {
            multiplesOfPrimeFactors.add(candidate);
            return false;
        }
        return isNotMultipleOfAnyPreviousPrimeFactor(candidate);
    }

    private static boolean
    isLeastRelevantMultipleOfLargerPrimeFactor(int candidate) {
        int nextLargerPrimeFactor =
            primes[multiplesOfPrimeFactors.size()];
        int leastRelevantMultiple =
            nextLargerPrimeFactor * nextLargerPrimeFactor;
        return candidate == leastRelevantMultiple;
    }

    private static boolean
    isNotMultipleOfAnyPreviousPrimeFactor(int candidate) {
        for (int n = 1; n < multiplesOfPrimeFactors.size(); n++) {
            if (isMultipleOfNthPrimeFactor(candidate, n))
                return false;
        }
        return true;
    }

    private static boolean
    isMultipleOfNthPrimeFactor(int candidate, int n) {
        return
            candidate ==
            smallestOddNthMultipleNotLessThanCandidate(candidate, n);
    }

    private static int
    smallestOddNthMultipleNotLessThanCandidate(int candidate, int n)
    {
        int multiple = multiplesOfPrimeFactors.get(n);
        while (multiple < candidate)
            multiple += 2 * primes[n];
        multiplesOfPrimeFactors.set(n, multiple);
        return multiple;
    }
}
```

在我们深入这段代码之前，我鼓励每个阅读这篇文章的人花时间阅读这段代码，并得出自己的结论。
你觉得这段代码容易理解吗？
如果是，为什么？
如果不是，什么使它复杂？

另外，Bob，你能确认你支持这段代码（即这段代码恰当地体现了《Clean Code》的设计理念，并且这是你认为如果它在生产中使用时应该出现的代码）吗？

**UB：**<br/>
啊，是的。
PrimeGenerator。
这段代码来自 Donald Knuth 1982 年的论文 “文学编程” ² 。
该程序最初是用 Pascal 编写的，由 Knuth 的 WEB 系统自动生成为一个单一的、非常大的方法，我将其翻译成了 Java。

² https://www.cs.tufts.edu/~nr/cs257/archive/literate-programming/01-knuth-lp.pdf

当然，这段代码从未打算用于生产。
Knuth 和我都把它作为一个教学示例。
在《Clean Code》中，它出现在一个名为 “类” 的章节中。
该章的教训是，一个非常大的方法通常包含许多不同的代码段，这些代码段最好分解成独立的类。

在该章中，我从那个函数中提取了三个类：PrimePrinter、RowColumnPagePrinter 和 PrimeGenerator。

其中提取的类之一是 PrimeGenerator。
它有以下代码（我没有在书中发布）。
变量名和整体结构是 Knuth 的。

```java
public class PrimeGenerator {
    protected static int[] generate(int n) {
        int[] p = new int[n];
        ArrayList<Integer> mult = new ArrayList<Integer>();
        p[0] = 2;
        mult.add(2);
        int k = 1;
        for (int j = 3; k < p.length; j += 2) {
            boolean jprime = false;
            int ord = mult.size();
            int square = p[ord] * p[ord];
            if (j == square) {
                mult.add(j);
            } else {
                jprime=true;
                for (int mi = 1; mi < ord; mi++) {
                    int m = mult.get(mi);
                    while (m < j)
                        m += 2 * p[mi];
                    mult.set(mi, m);
                    if (j == m) {
                        jprime = false;
                        break;
                    }
                }
            }
            if (jprime)
                p[k++] = j;
        }
        return p;
    }
}
```

尽管我已经完成了该章的教训，但我不想让那个方法看起来如此过时。
所以我作为事后想法稍微清理了一下。
我的目标不是描述如何生成质数。
我想让我的读者看到，违反单一职责原则的大型方法如何可以被分解为几个更小的、命名良好的类，其中包含几个更小的、命名良好的方法。

**JOHN：**<br/>
感谢提供背景信息。
尽管那段代码的细节不是该章的重点，但大概该代码代表了你认为的 “正确” 和 “最整洁” 的做法，考虑到手头的算法。
而这就是我不同意的地方。

PrimeGenerator 有许多设计问题，但现在我将专注于方法长度。
代码被切分得太多（八个微小的方法），以至于难以阅读。
首先，考虑 `isNotMultipleOfAnyPreviousPrimeFactor` 方法。
这个方法调用 `isMultipleOfNthPrimeFactor`，后者又调用 `smallestOddNthMultipleNotLessThanCandidate`。
这些方法是浅层且纠缠的：为了理解 `isNot...`，你必须阅读其他两个方法，并一次性将所有这些代码加载到你的脑海中。
例如，`isNot...` 有副作用（它修改 `multiplesOfPrimeFactors`），但除非你阅读所有三个方法，否则你看不到这一点。

**UB：**<br/>
我认为你说得有道理。
十八年前，当我沉浸在这项重构中时，这些名称和结构对我来说完全合理。
它们现在对我来说也合理 —— 但那是因为我再次理解了算法。
当我几天前第一次回到这个算法时，我在名称和结构上挣扎。
一旦我理解了算法，名称和结构就完全合理了。

**JOHN：**<br/>
即使对于理解算法的人来说，那些名称也是成问题的；我们将在讨论注释时稍后讨论它们。
如果代码在作者后来返回时不再对作者有意义，这意味着代码是有问题的。
代码最终可以被理解（伴随着巨大的痛苦和折磨）这一事实并不能原谅它的纠缠。

**UB：**<br/>
要是我们有那样的水晶球，可以帮助未来的自己避免那种 “巨大的痛苦和折磨”。;-)

**JOHN：**<br/>
不需要水晶球。
PrimeGenerator 的问题是相当明显的，比如纠缠和接口复杂性；也许你对它难以理解感到惊讶，但我并不惊讶。
换句话说，如果你无法预测你的代码是否易于理解，那你的设计方法论就有问题。

**UB：**<br/>
说得对。
然而，我会说，我在解读你的重写（下面）时也经历了同样的 “痛苦和折磨”。
所以，显然，我们的方法论都不足以将我们的读者从这种挣扎中解救出来。

**JOHN：**<br/>
回到我关于复杂性的介绍性评论，将 `isNot...` 拆分为三个方法并不能减少你必须记在脑子里的信息量。
它只是把信息分散开了，使得你需要一起阅读所有三个方法这一事实不那么明显了。
而且更难看到代码的整体结构，因为它被拆分开了：读者不得不在方法之间来回翻阅，实际上是在脑海中重建一个单体版本。
因为这些部分都是相互关联的，如果它们都在一个地方，这段代码将最容易理解。

**UB：**<br/>
我不同意。
这是 `isNotMultipleOfAnyPreviousPrimeFactor`。

```java
private static boolean
isNotMultipleOfAnyPreviousPrimeFactor(int candidate) {
    for (int n = 1; n < multiplesOfPrimeFactors.size(); n++) {
        if (isMultipleOfNthPrimeFactor(candidate, n))
            return false;
    }
    return true;
}
```

如果你信任 `isMultipleOfNthPrimeFactor` 方法，那么这个方法本身就很好了。
我的意思是，我们循环遍历所有 N 个以前的质数，看看候选数是否是倍数。
这是相当直接的。

现在，问我们如何确定候选数是否是倍数的问题，在这种情况下，你会想检查 `isMultiple...` 方法。

**JOHN：**<br/>
这段代码看起来确实简单明了。
不幸的是，这种表象是欺骗性的。
如果读者信任 `isMultipleOfNthPrimeFactor` 这个名字（它暗示一个没有副作用的谓词），并且不费心去阅读它的代码，他们就不会意识到它有副作用，而且这些副作用对 `isNot...` 的候选参数施加了一个约束（它必须在每次调用之间单调非递减）。
要理解这些行为，你必须阅读 `isMultiple...` 和 `smallestOdd...`。
当前的分解向读者隐藏了这个重要信息。

如果说有什么比不理解代码更可能导致错误的话，那就是当你其实不理解时，却以为自己理解了。

**UB：**<br/>
这是一个合理的担忧。
然而，它被这样一个事实所缓和：函数是按照它们被调用的顺序呈现的。
因此，我们可以预期读者已经看到了主循环，并理解候选数在每次迭代中增加二。

埋藏在 `smallestOddNth...` 中的副作用更成问题。
既然你指出了它，我就不太喜欢它了。
尽管如此，那个副作用不应该混淆 `isNot...` 的基本理解。

总的来说，如果你信任被调用方法的名称，那么理解调用者就不需要理解被调用者。
例如：

```java
for (Employee e : employees)
    if (e.shouldPayToday())
        e.pay();
```

如果我们用它们的实现替换那两个方法调用，这不会使它更易于理解。
这样的替换只会混淆意图。

**JOHN：**<br/>
这个例子之所以有效，是因为被调用的方法相对独立于父方法。
不幸的是，`isNot...` 的情况并非如此。

事实上，`isNot...` 不仅与它调用的方法纠缠，它还与其调用者纠缠。
`isNot...` 只有在它被一个候选数单调递增的循环中调用时才有效。
要说服自己它有效，你必须找到调用 `isNot...` 的代码，并确保候选数在每次调用之间从不减少。
将 `isNot...` 与调用它的循环分开，使得读者更难说服自己它有效。

**UB：**<br/>
正如我之前所说，这就是为什么方法按照它们的方式排序。
我期望，到你看 `isNot...` 时，你已经阅读了 `checkOddNumbersForSubsequentPrimes`，并知道候选数以二递增。

**JOHN：**<br/>
让我们简要讨论一下，因为这是另一个我不同意《Clean Code》的领域。
如果方法是纠缠的，没有巧妙的方法定义顺序可以解决问题。

在这种特定情况下，在两个方法 `checkOdd...` 中的循环和 `isNot...` 之间插入了其他两个方法，所以读者在到达 `isNot...` 之前会忘记循环上下文。
而且，创建对循环依赖的实际代码不在 `isNot...` 中：它在 `smallestOdd...` 中，这离 `checkOdd...` 更远了。

**UB：**<br/>
我真诚地怀疑有人会忘记候选数以二递增。
这是一种避免浪费的相当明显的方式。

**JOHN：**<br/>
在我的开场白中，我谈到了减少人们必须一次在脑海中保留的信息量是多么重要。
在这种情况下，读者必须在阅读四个大部分与循环无关的中间方法时记住那个循环。
你显然认为这将是容易和自然的（我不同意）。
但情况更糟。
没有迹象表明 `checkOdd...` 的哪些部分以后会重要，所以唯一安全的方法是从每个方法记住所有内容，直到你遇到所有可能从它派生的其他方法。
而且，为了在片段之间建立联系，读者还必须重构调用图，以注意到即使通过四层方法调用，`smallestOdd...` 中的代码也对 `checkOdd...` 中的循环施加了约束。
这是对读者的一种不合理的认知负担。

如果两段代码是紧密相关的，解决方案是将它们放在一起。
将片段分开，即使是在物理上相邻的方法中，也会使代码更难理解。

对我来说，PrimeGenerator 中的所有方法都是纠缠的：为了理解这个类，我必须同时将它们全部加载到我的脑海中。
我在阅读代码时不断在方法之间来回翻阅。
这是一个危险信号，表明代码已被过度分解。

Bob，你能帮我理解为什么你把代码分成如此微小的方法吗？
拥有这么多方法有什么我没有看到的好处吗？

**UB：**<br/>
我想你我只是在这个问题上无法达成一致。
总的来说，我相信小的、命名良好的方法和关注点分离的原则。
一般来说，如果你能将一个大方法分解为几个具有不同关注点的、命名良好的较小方法，并通过这样做暴露它们的接口和高层功能分解，那么这是一件好事。

- 循环遍历奇数是一个关注点。
- 确定素性是另一个。
- 标记质数的倍数又是另一个。

在我看来，分离和命名这些关注点有助于揭示算法的工作方式 —— 即使以一些纠缠为代价。

在你的解决方案中，我们很快将在下面看到，你以类似的方式分解了算法。
然而，你不是将关注点分离成函数，而是将它们分离成带有注释的部分。

你提到，在我的解决方案中，读者在阅读其他函数时必须记住循环上下文。
我认为，在你的解决方案中，读者在阅读你的解释性注释时必须记住循环上下文。
他们可能不得不在各部分之间 “来回翻阅” 以建立他们的理解。

现在，也许你担心在我的解决方案中，“翻阅” 的距离（以行为单位）比你的要长。
我不确定这是一个重要的问题，因为它们都适合在同一个屏幕上（至少在我的屏幕上是这样），而且地标是相当明显的。
