## Bob 对 PrimeGenerator2 的重写

**UB：**<br/>
当我看到你的解决方案，并且我对它有很好的理解之后，我稍微重构了一下它。
我把它加载到我的 IDE 中，写了一些简单的测试，并提取了一些简单的方法。

我也去掉了那个糟糕的带标签的 `continue` 语句。
我把 3 加入质数列表，这样我可以把第一个元素标记为无关紧要，并给它一个值 -1。
（我想我还在为偶/奇混淆而晕头转向。）

我喜欢这个，因为 `generateFirstNPrimes` 方法的实现以一种暗示正在发生的事情的方式描述了活动部件。
阅读那个实现很容易，并能瞥见其机制。
我完全不确定注释是否有帮助。

我认为这个算法的现实是，正确解释它所需的工作，以及任何其他人阅读和理解那个解释所需的工作，大致相当于阅读代码和骑自行车所需的工作。

```java
package literatePrimes;

public class PrimeGenerator3 {
    private static int[] primes;
    private static int[] primeMultiples;
    private static int lastRelevantMultiple;
    private static int primesFound;
    private static int candidate;

    // Lovely little algorithm that finds primes by predicting
    // the next composite number and skipping over it. That prediction
    // consists of a set of prime multiples that are continuously
    // increased to keep pace with the candidate.
    public static int[] generateFirstNPrimes(int n) {
        initializeTheGenerator(n);
        for (candidate = 5; primesFound < n; candidate += 2) {
            increaseEachPrimeMultipleToOrBeyondCandidate();
            if (candidateIsNotOneOfThePrimeMultiples()) {
                registerTheCandidateAsPrime();
            }
        }
        return primes;
    }

    private static void initializeTheGenerator(int n) {
        primes = new int[n];
        primeMultiples = new int[n];
        lastRelevantMultiple = 1;
        // prime the pump. (Sorry, couldn't resist.)
        primesFound = 2;
        primes[0] = 2;
        primes[1] = 3;
        primeMultiples[0] = -1;// irrelevant
        primeMultiples[1] = 9;
    }

    private static void increaseEachPrimeMultipleToOrBeyondCandidate() {
        if (candidate >= primeMultiples[lastRelevantMultiple])
            lastRelevantMultiple++;
        for (int i = 1; i <= lastRelevantMultiple; i++)
            while (primeMultiples[i] < candidate)
                primeMultiples[i] += 2 * primes[i];
    }

    private static boolean candidateIsNotOneOfThePrimeMultiples() {
        for (int i = 1; i <= lastRelevantMultiple; i++)
            if (primeMultiples[i] == candidate)
                return false;
        return true;
    }

    private static void registerTheCandidateAsPrime() {
        primes[primesFound] = candidate;
        primeMultiples[primesFound] = candidate * candidate;
        primesFound++;
    }
}
```

**JOHN：**<br/>
这个版本比《Clean Code》中的版本有了相当大的改进。
减少方法数量使代码更容易阅读，并产生了更清晰的接口。
如果有适当的注释，我认为这个版本和我的一样容易阅读（你创建的额外方法没有特别帮助，但也没有损害）。
我怀疑如果我们对读者进行调查，有些人会喜欢你的版本，有些人会喜欢我的。

不幸的是，这个代码修订造成了严重的性能回归：我测量到比之前任何一个修订版本慢了 3 到 4 倍。
问题在于你把对特定候选数的处理从单个循环改成了两个循环（`increaseEach...` 和 `candidateIsNot...` 方法）。
在之前修订版本的循环中，以及在 `candidateIsNot` 方法中，一旦候选数被排除，循环就会中止（大多数候选数很快被排除）。
然而，`increaseEach...` 必须检查 `primeMultiples` 中的每一个条目。
这导致循环迭代次数增加了 5 到 10 倍，整体慢了 3 到 4 倍。

鉴于当前算法（及其复杂性）的全部原因是为了最大化性能，这种减速是不可接受的。
这两个方法必须合并。

我认为这里发生的事情是，你如此专注于实际上并不那么重要的事情（创建尽可能小的方法），以至于你在其他真正重要的问题上失误了。
我们现在已经看到了两次。
在 PrimeGenerator 的原始版本中，你如此决心要创建微小的方法，以至于你没有注意到代码变得难以理解。
在这个版本中，你如此渴望拆分我的单一方法，以至于你没有注意到你正在破坏性能。

我不认为这只是一个不幸的疏忽组合。
软件设计中最重要的的事情之一是识别什么是重要的并专注于它；如果你专注于不重要的事情，你很可能会搞砸重要的事情。

你修订中的代码仍然缺乏注释。
你认为没有有意义的方式让注释帮助读者理解代码。
我认为这源于你对评论价值的普遍不信任；你很快就放弃了。
这个算法异常难以解释，但我仍然相信注释可以帮助。
例如，我相信你必须做出一些努力来帮助读者理解为什么一个质数的第一个倍数是质数的平方。
你已经花了大量时间来发展你对这个的理解；肯定有某种方式可以将这种理解传达给他人？
如果你在代码的原始版本中包含了那个信息，你可以省去那趟漫长的自行车骑行。
放弃这一点是对专业责任的放弃。

你在修订中包含的几个注释价值不大。
第一个注释太神秘了，没有提供太多帮助：我无法理解 “预测下一个合数并跳过它” 这句话的意思，尽管我完全理解它声称要解释的代码。
其中一个注释只是一个玩笑；鉴于你反对无关的注释，看到这一点我很惊讶。

显然，在注释方面，你和我生活在不同的宇宙。

最后，我不明白你为什么被我代码中的带标签的 `continue` 语句冒犯。
这是从嵌套循环中退出的一个干净而优雅的解决方案。
我希望更多的语言有这个功能；替代方案是尴尬的代码，你设置一个变量，然后退出一个循环级别，然后检查变量并退出下一个级别。

**UB：**<br/>
好发现！
如果我想到要分析解决方案，我也会发现这一点。
你说得对，将两个循环分开增加了一些不必要的迭代。
我找到了一个解决这个问题的好方法，而不使用那个可怕的 `continue`。
我更新后的版本现在比你的更快！
一百万质数在 440 毫秒内，而你的需要 561 毫秒。
;-) 下面只是更改。

```java
public static int[] generateFirstNPrimes(int n) {
    initializeTheGenerator(n);
    for (candidate = 5; primesFound < n; candidate += 2)
        if (candidateIsPrime())
            registerTheCandidateAsPrime();
    return primes;
}

private static boolean candidateIsPrime() {
    if (candidate >= primeMultiples[lastRelevantMultiple])
        lastRelevantMultiple++;
    for (int i = 1; i <= lastRelevantMultiple; i++) {
        while (primeMultiples[i] < candidate)
            primeMultiples[i] += 2 * primes[i];
        if (primeMultiples[i] == candidate)
            return false;
    }
    return true;
}
```

**JOHN：**<br/>
是的，这解决了问题。
我注意到你现在从《Clean Code》版本中的八个方法减少到四个方法。
