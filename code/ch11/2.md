## 步降规则：再来一次

所以，为了保持礼貌，我喜欢遵循的规则是每次下降一个抽象层次。
每个函数都有一个定义函数做什么的名称，每个函数由一组描述实现的代码行组成，并且所有这些代码行都处于相同的抽象层次。
如果这些行中的每一行尚未处于最低层，则调用下一层的函数。

<ins>你如何确定一个抽象层次有多大？
我只能说，用你最好的判断。
对我来说，这很简单。
如果我能有意义地提取，我就进行提取并检查它是否有意义</ins>。

这是一种礼貌的做法，它有助于避免抽象过山车 (abstraction roller coaster)。
