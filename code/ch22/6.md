## 保持同步部分小

`synchronized` 关键字引入了一个锁。
由同一锁保护的所有代码段在任何给定时间都保证只有一个线程执行它们。
锁很昂贵，因为它们会创建延迟并增加开销。
所以我们不想用 `synchronized` 语句来乱丢我们的代码。
另一方面，临界区 ⁸ 必须被保护。
所以我们希望用尽可能少的临界区来设计我们的代码。

⁸ 临界区是任何必须被保护以防止同时使用的代码段，以便程序正确。

一些天真的程序员试图通过使他们的临界区非常大来实现这一点。
然而，将同步扩展到超出最小临界区会增加争用并降低性能。

**建议** ：保持你的同步部分尽可能小。
